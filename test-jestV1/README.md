# Andrew Polovets Jest for API

### Key Artifacts:
* ./.env
* ./tests

### Preconditions:
* Optionally configure .env file with `SPEC_NAMES` without extension separated by comma
  * `SPEC_NAMES=first,second`

### Commands:
* To run test/s:
    * npm t
